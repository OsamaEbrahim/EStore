@model EStore.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <h4>Recently Added:</h4>
    <div class="row">
        @foreach (var product in Model.RecentlyAdded)
        {
            <div class="col-md-4">
                <div class="card mb-4 border-secondary" style="width: 18rem;">
                    @foreach (var pic in product.ImagesPaths)
                    {

                        <img class="card-img-top" src="~/@pic.Path" alt="Card image cap" width="250" height="250">

                    }
                    <div class="card-body">
                        <h5 class="card-title">@product.Name</h5>
                        <h6 class="card-subtitle mb-2 text-muted">@product.Price BD</h6>
                        <p class="card-text">@product.Description.</p>
                        <button class="btn btn-primary" onclick="AddToCart(@product.ProductId)">Add To Cart</button>
                        <a asp-controller="Products" asp-action="ProductPage" asp-route-name="@product.Name" class="btn btn-primary">View</a>
                    </div>
                </div>
            </div>
        }
    </div>
    <hr />
    <h4>Most Selling:</h4>
    <div class="row">
        @foreach (var product in Model.MostSelling)
        {
            <div class="col-md-4">
                <div class="card mb-4 border-secondary" style="width: 18rem;">
                    @foreach (var pic in product.ImagesPaths)
                    {

                        <img class="card-img-top" src="~/@pic.Path" alt="Card image cap" width="250" height="250">

                    }
                    <div class="card-body">
                        <h5 class="card-title">@product.Name</h5>
                        <h6 class="card-subtitle mb-2 text-muted">@product.Price BD</h6>
                        <p class="card-text">@product.Description.</p>
                        <button class="btn btn-primary" onclick="AddToCart(@product.ProductId)">Add To Cart</button>
                        <a asp-controller="Products" asp-action="ProductPage" asp-route-name="@product.Name" class="btn btn-primary">View</a>
                    </div>
                </div>
            </div>
        }
    </div>
    <hr />
    <h4>Low In Stock:</h4>
    <div class="row">
        @foreach (var product in Model.LowInStock)
        {
            <div class="col-md-4">
                <div class="card mb-3 border-secondary" style="width: 18rem;">
                    @foreach (var pic in product.ImagesPaths)
                    {

                        <img class="card-img-top" src="~/@pic.Path" alt="Card image cap" width="250" height="250">

                    }
                    <div class="card-body">
                        <h5 class="card-title">@product.Name</h5>
                        <h6 class="card-subtitle mb-2 text-muted">@product.Price BD</h6>
                        <p class="card-text">@product.Description.</p>
                        <button class="btn btn-primary" onclick="AddToCart(@product.ProductId)">Add To Cart</button>
                        <a asp-controller="Products" asp-action="ProductPage" asp-route-name="@product.Name" class="btn btn-primary">View</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
